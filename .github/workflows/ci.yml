name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: uv sync --extra dev
      - run: uv run ruff check app_store_connect_mcp tests
      - run: uv run mypy app_store_connect_mcp
      - run: uv run pytest --cov=app_store_connect_mcp --cov-report=term-missing --cov-fail-under=65
